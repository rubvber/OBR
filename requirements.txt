# This file may be used to create an environment using:
# $ conda create --name <env> --file <this file> -c pytorch -c defaults -c conda-forge
# (Note the order of the conda channels! Altering this may lead to pytorch installing 
# without GPU support, or cause the conda install to get stuck at the "solving environment" stage.)
cudatoolkit=11.3.1
matplotlib=3.5.2
numpy=1.22.3
pillow=9.2.0
pycairo=1.21.0
python=3.10.4
pytorch=1.12.1
pytorch-lightning=1.7.3
scikit-learn=1.1.1
torchaudio=0.12.1
torchvision=0.13.1
